package cz.pflanzer.foldduo

/** Debug source set supplies the broadcast-driven icon style switch. */
internal object IconStyleDebug {
    fun attach(activity: MainActivity, model: LauncherModel) = Unit
}
