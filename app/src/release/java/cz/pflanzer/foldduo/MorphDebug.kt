package cz.pflanzer.foldduo

/** Debug source set supplies the broadcast-driven replay of the unfold morph. */
internal object MorphDebug {
    fun attach(activity: MainActivity, morph: MorphController) = Unit
}
